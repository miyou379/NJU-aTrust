on:
  push:
    branches: [main]

jobs:
  rpm-build:
    runs-on: ubuntu-24.04
    container:
      image: fedora:latest
    steps:
      - uses: actions/checkout@v4
      - run: dnf install -y wget rpm-build redhat-rpm-config rpmautospec
      - run: dnf builddep -y ./atrust-rpm.spec
      - run: sh ./build.sh
      - run: ls | grep "aTrust-*"
    
